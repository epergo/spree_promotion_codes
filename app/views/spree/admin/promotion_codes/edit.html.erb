<%= render partial: 'spree/admin/shared/error_messages', locals: { target: @promotion_code } %>

<%= form_for [:admin, @promotion, @promotion_code] do |f| %>
  <fieldset>
    <div data-hook="admin_promotion_code_edit_form">
      <%= render partial: 'form', locals: { f: f } %>
    </div>

    <%= render partial: 'spree/admin/shared/edit_resource_links' %>
  </fieldset>
<% end %>

